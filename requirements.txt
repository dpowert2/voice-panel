# Voice Panel — runtime dependencies.
# We deliberately don't use the anthropic SDK or livekit-agents — both have
# heavy import trees. Direct HTTP via aiohttp + urllib keeps boot in <2s.

aiohttp>=3.10
python-dotenv>=1.0

# Pipecat — used by the `cartesia-agents` mode (see pipecat_panel.py).
# Only the Cartesia TTS service is exercised; we keep the install slim by
# requesting just the `cartesia` extra (no Daily/WebRTC/Deepgram/OpenAI).
# pipecat_panel.py has a graceful fallback if this import fails at runtime
# (it routes to the same Cartesia WS protocol directly), so a build hiccup
# on Render free-tier won't take down `cartesia-agents` mode.
pipecat-ai[cartesia]>=1.3.0
